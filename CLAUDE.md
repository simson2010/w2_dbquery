# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

w2-dbquery is a Python project (requires Python 3.12+) managed with uv.

## Commands

```bash
# Run the application
uv run python main.py

# Add dependencies
uv add <package>

# Sync dependencies
uv sync
```

## Architecture

Currently a minimal project with a single entry point in `main.py`.
